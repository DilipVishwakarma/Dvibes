# TODO - Playlist dashboard view (in main Popular Songs section)

- [ ] Step 1: Update `assets/js/app.js` to add new dashboard mode state + method `loadDashboardPlaylist(playlistId, playlistName)`.
- [ ] Step 2: In `assets/js/app.js`, wire clicks on sidebar playlist buttons (rendered by `loadUserPlaylists()`) to call `loadDashboardPlaylist(...)` instead of opening the playlist-detail panel.
- [ ] Step 3: Ensure `goHome()` resets dashboard mode back to popular and restores the home markup.
- [ ] Step 4: Make sure current playback is not stopped when switching dashboard playlist queue.
- [ ] Step 5: Validate next/prev repeat/shuffle behavior within playlist queue.
- [ ] Step 6: Basic manual testing checklist.

